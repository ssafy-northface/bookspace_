# Frontend (Vue 3)
Scaffold with Vite, then wire folders:
- components/common, components/domain, iews, outer, stores (Pinia), pi, composables, styles.
Run 
pm create vite@latest frontend -- --template vue-ts or similar, then move/merge this structure.
